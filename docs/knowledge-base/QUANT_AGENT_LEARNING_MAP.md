# 量化智能体知识库

## 1. 我们现在该怎么理解项目

这个项目已经不是单纯的“量化算法集合”，而是一个可以被智能体调用的量化能力平台。

同时，我们也明确它的使用定位：

- 这是一个私人私用的股票预测与推荐智能体
- 目标是快速投产、真实测试、持续迭代
- 我们更看重实际效果和可用性，而不是一开始把每个地方都做到极致
- 先把算法、工具和工作流在真实场景里跑起来，再根据生产反馈逐步完善

核心判断：

- 智能体负责编排和解释
- 算法工具负责计算和验证
- 数据层负责稳定、降级和追踪
- 回测层负责复验，不能只靠模型口头判断

---

## 2. 最值得借鉴的开源项目

### 2.1 Qlib

来源：

- [Qlib](https://github.com/microsoft/qlib)
- [Qlib-Server](https://github.com/microsoft/qlib-server)

值得学的地方：

- 研究流程到生产流程的分层
- 数据集、特征和实验的标准化
- 离线研究和在线服务的分离

适合我们落地到：

- 数据提供器抽象
- 缓存与数据版本管理
- 特征和实验记录

### 2.2 vn.py

来源：

- [vn.py](https://github.com/vnpy/vnpy)

值得学的地方：

- 框架边界清晰
- Gateway / Engine / Strategy 分层明确
- 适合扩展到实盘接口

适合我们落地到：

- 交易通道抽象
- 策略执行引擎
- 实盘扩展预留接口

### 2.3 TradingAgents

来源：

- [TradingAgents](https://github.com/TauricResearch/TradingAgents)

值得学的地方：

- 多角色分工
- 把复杂交易决策拆成研究、观点、风控和结论
- 强调这是研究框架，不是自动投资承诺

适合我们落地到：

- 多智能体编排
- 让 agent 做“选择工具”和“组织结论”
- 风控和结论分离输出

### 2.4 FinRobot

来源：

- [FinRobot](https://github.com/AI4Finance-Foundation/FinRobot)
- [FinRobot Equity Research Module](https://github.com/AI4Finance-Foundation/FinRobot/blob/master/finrobot_equity/README.md)

值得学的地方：

- 金融场景下的多模型统一编排
- 研究、报告、风控的整体化
- 面向股票研究的完整工作流

适合我们落地到：

- 统一分析报告生成
- 模型 + 规则 + 风控的组合
- 报告可追溯

---

## 3. 我们项目里可以直接复用的能力

### 3.1 已经适合给智能体调用的工具

- `analyze_stock`
- `recommend_by_risk`
- `get_market_overview`
- `get_risk_profile`
- `run_backtest`
- `generate_stock_report`

### 3.2 已经适合继续增强的工具

- `recommend_long_term`
- `recommend_short_term`
- `screen_stocks`
- `analyze_trading_decision`
- `get_financial_indicators`

### 3.3 已经具备的工程基础

- 东方财富数据源与降级路径
- 回测引擎
- 风险管理
- 个股分析
- 基本面分析
- 市场分析
- 推荐器

---

## 4. 现在不建议走的方向

- 不建议把每个指标都直接暴露给智能体
- 不建议让模型自己拼接原始数据后直接下结论
- 不建议把所有能力都做成平级工具
- 不建议再新建一批重复的“长线/短线/稳健/保守”碎工具

原因：

- 工具太碎，模型容易乱路由
- 输出太散，不利于后续审计和回测
- 业务级工具比原子工具更稳定

---

## 5. 推荐的下一阶段方向

### 5.1 先做统一编排层

目标：

- 一个入口串起分析、推荐、回测、报告、风控

候选模块：

- `StockOrchestrator`
- `DataQualityChecker`
- `CacheManager`
- `AuditLogger`

### 5.2 再做统一输出格式

建议固定字段：

- `ok`
- `tool`
- `category`
- `data_source`
- `summary`
- `data`
- `meta`

### 5.3 再做模型和规则共存

建议策略：

- 规则负责解释性
- ML 负责排序和补充
- 风控负责约束
- 智能体负责组织表达

---

## 6. 推荐的技能生态使用方式

当前更适合复用的不是“金融特化 skill 大包”，而是通用能力：

- 规划类 skill
- 文档类 skill
- 代码审查类 skill
- 测试类 skill

金融特化 skill 可以作为参考，但不建议盲目作为主依赖，因为成熟度和安装量差异很大。

我们现在更应该优先把自己的知识库和工具边界做稳。

---

## 7. Phase 5 的开发原则

1. 先收口，再扩展
2. 先统一，再丰富
3. 先可回溯，再自动化
4. 先业务级工具，再原子工具
5. 先真实数据，真实不可用时明确降级

---

## 8. 结论

这个项目后续最好的方向不是“继续堆算法”，而是：

- 用智能体把现有能力串起来
- 用知识库把经验沉淀下来
- 用统一输出把结果规范起来
- 用回测和审计把系统稳定下来

这样才会越来越像一个可持续演进的量化智能体平台。

---

## 9. 本阶段新增沉淀：真实数据降级闭环

### 9.1 这次我们确认了什么

- 智能体层最重要的不是直接访问原子算法，而是访问稳定的业务级工具
- 数据层必须显式标记真实、缓存、模拟和配置来源
- 真实数据失败时，系统应返回受控降级结果，而不是把异常直接暴露给智能体
- 编排层应继续承担“路由 + 追踪 + 统一输出”的职责

### 9.2 本阶段已经落地的方向

- 东方财富行情新增治理快照入口
- akshare 基本面新增治理快照入口
- 分析与回测入口开始读取统一治理信息
- 规格中补充了真实数据降级的独立草案

### 9.3 对后续开发的建议

后续最值得继续做的是：

1. 把市场概览、风控、回测结果全部纳入统一治理输出
2. 把工具输出进一步标准化为更严格的 schema
3. 在智能体侧引入更明确的自动路由规则
4. 再考虑把外部数据源、第三方 skill 和内部 skill 做分层编排

---

## 10. 本阶段新增沉淀：统一输出治理

### 10.1 我们确认的原则

- 市场概览、风控、回测不应成为和分析、推荐不同口径的第二套系统
- 统一输出比继续拆原子能力更重要
- 智能体最需要的是稳定 schema，而不是更多碎工具

### 10.2 本阶段落地内容

- 工具层统一治理包装
- 回测结果补充 governance
- 编排层继续透传治理信息
- 统一输出规格新增

### 10.3 对下一阶段的建议

下一阶段更适合做：

---

## 11. 本阶段新增沉淀：最小 CLI 启动方式

### 11.1 我们确认的原则

- 生产可用不等于一开始就做复杂部署体系
- 对当前项目来说，最有价值的是把核心 HTTP API 做成一条命令可启动
- 启动入口要稳定、少参数、易安装，方便后续挂接到容器或进程守护

### 11.2 本阶段落地内容

- 新增包内 CLI 入口 `core/cli.py`
- 新增脚本 `tools/agent-api.py`
- 注册安装命令 `stock-agent-api`
- 保留 `host` / `port` 最小参数

### 11.3 对下一阶段的建议

下一阶段更适合做：

1. 写一个最小调用示例，方便联调 `/decision`
2. 在真实环境验证启动、健康检查和反馈闭环
3. 如有需要，再补一个极简配置文件或部署模板

---

## 12. 本阶段新增沉淀：最小 API 联调用例

### 12.1 我们确认的原则

- 联调示例应该和服务本体同样轻
- 最好不用第三方 HTTP 库，减少环境依赖和排障成本
- 示例要覆盖真实联调路径，而不是只做打印演示

### 12.2 本阶段落地内容

- 新增 `examples/api_demo.py`
- 覆盖健康检查、统一决策和可选反馈
- 输出保持 JSON 结构化，方便排错和观察结果

### 12.3 对下一阶段的建议

下一阶段更适合做：

1. 把这个示例写进 README 或使用说明
2. 在真实环境演示一次完整闭环
3. 再考虑加最小部署模板或 Docker 入口

---

## 13. 本阶段新增沉淀：最小运行说明

### 13.1 我们确认的原则

- 真正能交付的项目，不只是有入口，还要有清晰的运行步骤
- README 应该优先服务“马上要用的人”，而不是只服务开发者自己
- 最小运行说明应该围绕启动、联调、反馈闭环，而不是扩展到完整运维体系

### 13.2 本阶段落地内容

- README_QUANT 增加最小 HTTP API 启动说明
- README_QUANT 增加联调用例说明
- README_QUANT 说明了默认端口和环境变量

### 13.3 对下一阶段的建议

下一阶段最值得继续做的是：

1. 如果准备生产部署，再补最小部署模板
2. 如果要联东方财富真实行情，再补 Cookie 与配置说明
3. 再考虑给 `/decision` 增加一个最小 curl 示例

---

## 14. 本阶段新增沉淀：最小 curl 调试方式

### 14.1 我们确认的原则

- 终端直调是最轻量、最通用的联调方式
- 对当前项目来说，`curl` 示例比再加一层客户端封装更实用
- 调试方式越少，排障成本越低

### 14.2 本阶段落地内容

- README 补充 `curl /health`
- README 补充 `curl /decision`
- README 补充 `curl /feedback`

### 14.3 对下一阶段的建议

下一阶段最值得继续做的是：

1. 如果准备部署，再补最小部署模板
2. 如果需要给团队交接，再补接口字段说明
3. 如果要真实联调东方财富，再补数据源校验流程

---

## 15. 本阶段新增沉淀：东方财富真实联调脚本

### 15.1 我们确认的原则

- 真实数据验证要尽量薄，避免把网络问题和业务问题混在一起
- cookie 支持临时注入，便于本机快速试错
- 真正需要排查的是接口可达性、cookie 失效和返回结构变化

### 15.2 本阶段落地内容

- 新增 `examples/eastmoney_live_check.py`
- 支持 `--cookie`、`--cookie-file` 和 `EASTMONEY_COOKIE`
- 验证历史行情、实时行情和治理快照

### 15.3 对下一阶段的建议

下一阶段最值得继续做的是：

1. 等用户本机跑完后，根据结果判断是 cookie、网络还是接口字段变化
2. 如果真实数据通了，再考虑把这个脚本收进 README 的“数据联调”章节
3. 如果真实数据还不通，再单独收敛出接口适配或降级策略

---

## 16. 本阶段新增沉淀：东方财富真实联调已验证

### 16.1 我们确认的结论

- 真实行情链路已经在本机 `quant` 环境验证通过
- 历史行情、实时行情、治理快照都可用
- 当前项目的真实数据接入是可行的，后续可以直接围绕智能体能力继续推进

### 16.2 本阶段落地内容

- 历史行情返回 10 条记录，数据质量检查通过
- 实时行情返回 5 条样例，字段结构正常
- cookie 文件注入方式可用

### 16.3 对下一阶段的建议

下一阶段最值得继续做的是：

1. 基于真实数据继续强化智能体对分析、推荐和风控的统一输出
2. 继续保持最小联调脚本作为后续接口变更的回归工具
3. 不再回到“先堆接口，后验证”的顺序，而是坚持先验证再扩展

### 11. 本阶段新增沉淀：统一任务协议

### 11.1 我们确认的方向

- 项目的下一步重点不应继续停留在单个模型或单个算法的堆叠
- 智能体真正需要的是统一的任务协议，而不是更多互不兼容的能力入口
- 协作计划、工作流结果、模板命中、审计回放都应该共享同一套结构语义

### 11.2 本阶段落地内容

- 新增统一任务协议草案
- 协作计划可转换为 `TaskPlan`
- 工作流结果可转换为 `TaskResult`
- 编排器和客户端输出增加 `task_protocol`

### 11.3 对下一阶段的建议

后续最值得继续推进的是：

1. 任务回放统计
2. 学习闭环和模板命中分析
3. 统一输出在更多入口中的复用
4. 智能体自动路由与任务协议进一步收口

---

## 12. 本阶段新增沉淀：全工具统一 Schema

### 12.1 我们确认的原则

- 工具层的统一输出是智能体稳定接入的前提
- 工具内部对象可以多样，但对外契约必须收敛
- `data_source` 和 `governance` 是智能体解释结果的关键元信息

### 12.2 本阶段落地内容

- `core/agent/tools.py` 统一了主要业务工具的包装方式
- 回测与风控等结果可以被统一消费
- 编排层继续透传治理字段

### 12.3 对下一阶段的建议

下一阶段最适合继续做：

1. 智能体意图路由收紧
2. 输出 schema 的字段枚举进一步规范
3. 多智能体协作草案

---

## 13. 本阶段新增沉淀：智能体意图路由

### 13.1 我们确认的原则

- 路由不应只靠一组分散关键词
- 意图应该先结构化，再进入工具调用
- 股票代码和风险偏好可以被显式抽取并作为默认参数

### 13.2 本阶段落地内容

- `ArkAgentClient` 增加结构化意图解析
- `StockAgentRuntime` 暴露 `parse_intent()`
- 回测、市场、风控、推荐等场景都能返回统一路由信息

### 13.3 对下一阶段的建议

下一阶段适合做：

1. `StockOrchestrator` 与智能体路由进一步对齐
2. 复合意图优先级更细化
3. 多智能体协作草案

---

## 14. 本阶段新增沉淀：统一任务协议

### 14.1 我们确认的原则

- 项目下一步的价值不在继续加碎工具，而在统一任务语义
- 智能体、编排器、工作流、模板和审计需要共享同一种协议
- 任务协议比单个工具更适合作为智能体接入层的长期契约

### 14.2 本阶段落地内容

- 新增 `core/agent/task_protocol.py`
- 协作计划可转换为 `TaskPlan`
- 工作流结果可转换为 `TaskResult`
- 编排器和客户端输出增加 `task_protocol`

### 14.3 对下一阶段的建议

下一阶段最值得继续推进的是：

1. 任务回放统计
2. 学习闭环和模板命中分析
3. 统一输出在更多入口中的复用
4. 智能体自动路由与任务协议进一步收口

---

## 15. 本阶段新增沉淀：任务回放与学习统计

### 15.1 我们确认的原则

- 项目需要的不只是执行能力，还需要回放和复盘能力
- `workflow_id` 是把规划、执行、模板和审计串起来的最小主键
- 轻量统计优先于重型训练，先让系统能被持续观察

### 15.2 本阶段落地内容

- 新增任务回放模块 `core/agent/replay.py`
- 新增 `get_workflow_replay`
- 新增 `get_workflow_learning_stats`
- 智能体工具层可以直接消费回放与统计结果

### 15.3 对下一阶段的建议

下一阶段最值得继续推进的是：

1. 让回放结果进入调试视图或前端页面
2. 用学习统计反哺路由规则和模板选择
3. 继续统一输出字段枚举
4. 再考虑更细的多智能体协作边界

---

## 16. 本阶段新增沉淀：统一输出字段契约

### 16.1 我们确认的原则

- 项目对外输出必须有稳定字段，不应依赖上层自己猜结构
- `ok/tool/category/data_source/summary/data/meta` 是当前最核心的统一工具输出契约
- `task_protocol`、`governance`、`workflow_id` 是更高一层的稳定扩展字段

### 16.2 本阶段落地内容

- 新增统一输出契约模块 `core/agent/output_contract.py`
- 序列化层开始显式依赖工具输出契约
- 统一输出字段集合开始固定化

### 16.3 对下一阶段的建议

下一阶段最值得继续推进的是：

1. 让更多技能入口复用同一套输出契约
2. 让调试视图和前端直接消费这些稳定字段
3. 再考虑自动生成 schema 或接口文档
4. 继续保持任务协议和回放统计的收口方向

---

## 17. 本阶段新增沉淀：决策会话与用户反馈闭环

### 17.1 我们确认的原则

- 项目下一步要从“可执行”走向“可反馈、可复盘、可持续优化”
- `session_id` 是产品化主键，`workflow_id` 是执行主键
- 用户反馈是学习信号，不是普通日志附录

### 17.2 本阶段落地内容

- 新增 `core/agent/session.py`
- 新增 `create_decision_session`
- 新增 `submit_decision_feedback`
- 新增 `get_decision_session_replay`
- 新增 `get_decision_session_stats`
- 客户端和运行时都可直接创建和回放会话

### 17.3 对下一阶段的建议

下一阶段最值得继续推进的是：

1. 把决策会话接入调试页或工作台 API
2. 用反馈结果反哺路由、模板和推荐
3. 让会话统计和 workflow 学习统计联动
4. 再考虑工作台展示层

---

## 18. 本阶段新增沉淀：统一决策入口与最小生产闭环

### 18.1 我们确认的原则

- 当前阶段最重要的是能快速投入生产，而不是继续扩内部设计
- 对外应尽量只提供一个统一决策入口
- 输出应业务可读，内部细节尽量少暴露

### 18.2 本阶段落地内容

- 新增 `StockOrchestrator.answer_decision_request()`
- 新增业务决策摘要构造
- `StockAgentRuntime` 可直接调用统一决策入口
- 统一入口会自动创建决策会话并保留追踪信息

### 18.3 对下一阶段的建议

下一阶段最值得继续推进的是：

1. 简单结果展示页或 API 适配
2. 最小采纳/不采纳反馈入口
3. 在真实用户流中验证统一入口
4. 继续减少内部层次的暴露

---

## 19. 本阶段新增沉淀：统一决策工具入口

### 19.1 我们确认的原则

- 当前阶段最有价值的是把统一决策能力变成一个明确的对外工具入口
- 用户和智能体都应该优先使用同一个入口，而不是记住一堆分散能力
- 对外输出继续保持业务可读，不强调内部链路

### 19.2 本阶段落地内容

- 新增 `answer_decision_request` 工具入口
- 默认系统提示已优先引导该入口
- 工具注册表已暴露统一决策入口

### 19.3 对下一阶段的建议

下一阶段最值得继续推进的是：

1. 给统一决策入口加一个最小 API 封装
2. 在真实用户流里验证它
3. 如果需要，再做一个极简结果展示层
4. 继续保持入口单一、路径最短

---

## 20. 本阶段新增沉淀：最小 HTTP API 封装

### 20.1 我们确认的原则

- 快速投产优先于引入新框架
- 对外接口只保留最必要的三个入口：决策、反馈、健康检查
- HTTP 层应当是薄层，不再增加业务逻辑

### 20.2 本阶段落地内容

- 新增 `core/api_server.py`
- 提供 `POST /decision`
- 提供 `POST /feedback`
- 提供 `GET /health`
- 核心包导出 API 服务入口

### 20.3 对下一阶段的建议

下一阶段最值得继续推进的是：

1. 做一个最小 CLI 启动脚本
2. 在真实环境里验证统一入口
3. 如果需要，再做一个极简结果展示层
4. 继续保持接口数量最少化








---

## 13. 本阶段新增沉淀：共享路由

### 13.1 我们确认的原则

- 智能体路由和编排器路由必须共用一份规则
- 路由结果本身就是可审计数据
- 一旦共享路由建立，后续新增意图只改一处

### 13.2 本阶段落地内容

- `core/agent/routing.py` 成为共享路由模块
- `ArkAgentClient` 和 `StockOrchestrator` 复用同一意图解析
- 编排结果中可看到 route 元信息

### 13.3 对下一阶段的建议

下一阶段更适合：

1. 复合意图优先级优化
2. 路由审计日志
3. 多智能体协作草案

---

## 14. 本阶段新增沉淀：复合意图优先级

### 14.1 我们确认的原则

- 用户一句话里可能同时包含多个意图
- 主路由应该按稳定优先级选出，而不是第一个命中就直接返回
- 候选意图要保留，方便审计和后续优化

### 14.2 本阶段落地内容

- `core/agent/routing.py` 支持候选意图和命中词
- 复合意图时按优先级选择主路由
- 路由结果保留 `candidates` 和 `matched_terms`

### 14.3 对下一阶段的建议

下一阶段适合继续做：

1. 路由审计日志
2. 多智能体协作草案
3. 复合意图的更细粒度解释

---

## 15. 本阶段新增沉淀：复合意图优先级

### 15.1 我们确认的原则

- 复杂自然语言里，多个意图可以同时出现
- 主意图应按稳定优先级裁决
- 候选意图和命中词要保留，方便解释和审计

### 15.2 本阶段落地内容

- 共享路由支持候选意图列表
- 共享路由支持命中词列表
- 复合意图时按优先级选主路由

### 15.3 对下一阶段的建议

下一阶段适合继续做：

1. 路由审计日志
2. 多智能体协作草案
3. 更细粒度的复合意图解释

---

## 16. 本阶段新增沉淀：路由审计日志

### 16.1 我们确认的原则

- 路由决策应当可回放、可审计
- 路由审计不应该和业务逻辑耦合
- JSONL 追加写入比复杂日志平台更适合作为当前阶段的轻量实现

### 16.2 本阶段落地内容

- 新增 `core/agent/audit.py`
- `ArkAgentClient` 会记录路由审计
- `StockOrchestrator` 会记录路由审计
- 最近路由可以通过 `recent_routes()` 查看

### 16.3 对下一阶段的建议

下一阶段适合继续做：

1. 多智能体协作草案
2. 路由审计与决策审计的统一
3. 更细粒度的复合意图解释

---

## 17. 本阶段新增沉淀：多智能体协作规划

### 17.1 我们确认的原则

- 复合意图不应该只返回一个主路由，而应该先产出协作计划
- 协作计划的核心不是“让模型自己想”，而是“让模型按照既定角色和顺序去组织现有能力”
- 主任务、支持任务、执行顺序和回退方案必须同时可见
- 协作规划应该建立在共享路由和审计之上，而不是绕开它们

### 17.2 本阶段落地内容

- 新增 `core/agent/collaboration.py`
- 智能体客户端可优先调用 `plan_collaboration`
- 编排器可直接生成协作计划
- 协作计划会写入审计日志
- 复杂请求可先规划再执行，减少“单点工具硬答”的情况

### 17.3 对下一阶段的建议

下一阶段最值得继续做的是：

1. 把协作计划真正变成执行编排，而不是只停留在规划
2. 统一路由审计、规划审计和执行审计
3. 给市场、风控、回测和报告建立更明确的协作契约

---

## 18. 本阶段新增沉淀：协作执行闭环与统一工作流 Schema

### 18.1 我们确认的原则

- 规划只是开始，真正提升项目价值的是把规划执行出来
- 工作流执行应复用既有业务工具，不再重复实现分析逻辑
- 工作流结果必须保留计划、节点、顺序、降级和审计信息
- `execute_workflow` 应成为复杂请求的首选入口，而不是再让模型拼单工具

### 18.2 本阶段落地内容

- 新增 `core/agent/workflow.py`
- 新增 `execute_workflow` 工具
- `ArkAgentClient` 会在复合意图时优先走工作流执行
- `StockOrchestrator` 支持 `execute_workflow()`
- 显式工作流关键词可以直接触发工作流执行
- 工作流结果与路由审计已打通

### 18.3 对下一阶段的建议

下一阶段最值得继续做的是：

1. 把工作流节点化，进一步细化执行状态和回退策略
2. 统一规划审计、执行审计与结果审计
3. 把更多高价值业务场景固化为模板工作流

---

## 19. 本阶段新增沉淀：统一工作流审计与节点级回退

### 19.1 我们确认的原则

- 审计不能只看路由，必须覆盖工作流生命周期
- 规划、执行、结果应该共享同一套 workflow_id 语义
- 节点 fallback 不是隐藏错误，而是显式的降级输出
- fallback 成功也必须标记 `is_degraded`

### 19.2 本阶段落地内容

- `RouteAuditLogger` 支持 workflow 级事件字段
- 工作流执行记录 `plan / step / result` 三类审计
- 审计日志可按 `workflow_id`、`phase`、`event_type` 过滤
- `run_backtest`、`get_market_overview`、`generate_stock_report` 已具备降级兜底
- 客户端、编排器都可以回放 workflow 事件

### 19.3 对下一阶段的建议

下一阶段最值得继续做的是：

1. 把工作流模板标准化，减少重复的手工工作流定义
2. 为更多业务节点补齐更具体的 fallback 策略
3. 把 workflow 审计结果接入看板或诊断视图

---

## 20. 本阶段新增沉淀：标准工作流模板

### 20.1 我们确认的原则

- 高频高价值的协作链路应该被固化成模板，而不是每次都临时拼装
- 模板的价值在于稳定的任务顺序、稳定的依赖关系和稳定的审计结果
- 智能体更适合做模板选择和结果组织，而不是直接拼接底层原子工具
- 模板命中原因必须可见，便于后续回放和优化

### 20.2 本阶段落地内容

- 新增 `core/agent/workflow_templates.py`
- 协作规划可以优先匹配标准工作流模板
- 工作流执行结果增加 `template_id`、`template_name`、`template_reason`、`template_hit`
- 新增 `list_workflow_templates` 作为模板目录查看入口
- 模板信息同步进入协作计划、工作流结果和审计元数据

### 20.3 对下一阶段的建议

下一阶段最值得继续做的是：

1. 给模板增加命中统计，找到最常用的高价值链路
2. 为模板补充更多明确的业务场景，比如盘前概览、复盘报告、组合体检
3. 继续保持“模板优先，原子工具兜底”的智能体接入方式

---

## 21. 本阶段新增沉淀：模板命中统计与优先级优化

### 21.1 我们确认的原则

- 模板不是“有就行”，还要知道“谁更常用、谁更稳定”
- 当多个模板都可命中时，必须有统一、可解释的裁决规则
- 统计能力要尽量轻量，优先服务于模板优化，而不是引入复杂基础设施
- 模板选择结果应该包含评分，方便后续回放和调参

### 21.2 本阶段落地内容

- 新增 `core/agent/template_metrics.py`
- 模板命中会记录到本地 JSONL 指标日志
- 新增 `get_workflow_template_stats` 作为模板统计入口
- 模板选择会输出 `template_score`
- 模板优先级与关键词、意图、工具匹配一起参与裁决

### 21.3 对下一阶段的建议

下一阶段最值得继续做的是：

1. 新增更多标准模板，比如盘前概览、组合体检、复盘报告
2. 给模板统计结果加上更完整的可视化或分析入口
3. 逐步把模板选择策略做成可配置，而不是硬编码

---

## 22. 本阶段新增沉淀：模型治理与发布门禁

### 22.1 我们确认的原则

- 模型不是“训练完就结束”，而是要有版本、特征、评估和回滚
- 治理层不应该改算法本身，而应该把模型元数据收口
- 发布门禁比继续堆更复杂的模型更重要，因为它决定结果是否可信
- 先做轻量治理，再考虑更重的 MLOps 体系

### 22.2 本阶段落地内容

- 新增 `core/model/governance.py`
- 新增特征清单与模型记录
- 新增发布评估与回滚接口
- `MLRecommender` 训练与保存开始写入治理元数据
- `OnlineLearner` 和 `ModelManager` 开始写入模型治理记录
- 智能体工具新增模型治理状态查询和发布评估

### 22.3 对下一阶段的建议

下一阶段最值得继续做的是：

1. 把发布评估阈值固化成更明确的模型族策略
2. 将模型治理和回测结果联动，形成更稳的发布门禁
3. 再考虑把治理结果接到更完整的可视化或告警体系

---

## 23. 本阶段新增沉淀：运行保障与监控告警

### 23.1 我们确认的原则

- 智能体最需要的是稳定的运行健康态，而不是更碎的原子指标
- 工具级埋点应该收口在统一分发层，而不是散落到每个业务函数里
- 工作流、编排、模型治理和工具调用都应共享同一套健康与告警语义
- 轻量 JSONL 记录比重型监控平台更适合当前阶段的快速闭环

### 23.2 本阶段落地内容

- 新增 `core/observability/monitoring.py`
- 新增 `get_runtime_health` 与 `evaluate_runtime_health`
- `ProjectToolRegistry.dispatch()` 开始记录工具成功率、失败率和延迟
- `WorkflowExecutor.execute()` 开始记录工作流级耗时、步骤数和回退数
- `StockOrchestrator` 开始记录路由、缓存命中和模型治理运行事件

### 23.3 对下一阶段的建议

下一阶段最值得继续做的是：

1. 为更关键的业务指标补充可配置阈值
2. 把健康态进一步拆成可视化摘要和诊断建议
3. 让告警和回放结果直接服务于下一轮智能体路由优化

---

## 24. 本阶段新增沉淀：智能体能力目录与自动路由升级

### 24.1 我们确认的原则

- 项目下一阶段最值得做的不是继续增加原子工具，而是让智能体先看懂项目“能做什么”
- 能力目录比能力堆叠更重要，因为它决定默认入口、辅助入口和回退入口
- 对外要先输出高价值能力地图，再让智能体去执行

### 24.2 本阶段落地内容

- 新增 `core/agent/capability_directory.py`
- 新增 `list_project_capabilities` 工具入口
- `ArkAgentClient`、`StockOrchestrator`、`StockAgentRuntime` 都能查看能力目录
- 路由已支持“项目能做什么 / 能力目录 / 工具目录 / 能力清单”类问题
- 统一输出已补充 `knowledge_base` 分类

### 24.3 对下一阶段的建议

下一阶段最值得继续做的是：

1. 将能力目录入口前置到更多交互链路
2. 继续把高价值任务固化成标准工作流模板
3. 优先做盘前概览、组合体检和复盘报告这三类最能推进项目的标准路径
4. 保持“规格-实现-归档”的节奏，不再回到局部碎片优化

---

## 25. 本阶段新增沉淀：盘前概览标准工作流

### 25.1 我们确认的原则

- 第一个高价值标准工作流应选择盘前概览，因为它最能把市场、风控、运行健康和推荐串成日常入口
- 标准工作流不是“更复杂”，而是“更稳定、更可复用、更快执行”
- 盘前概览的价值在于先给结论，再给依据，适合快速投入生产

### 25.2 本阶段落地内容

- 新增 `specs/SDD_PHASE19_PRE_MARKET_OVERVIEW.md`
- 新增 `core/agent/workflow_templates.py` 中的 `盘前概览模板`
- `core/agent/routing.py` 可识别盘前类意图
- `core/agent/collaboration.py` 可生成固定执行顺序
- `core/agent/capability_directory.py` 增加盘前概览标准路径

### 25.3 对下一阶段的建议

下一阶段最值得继续做的是：

1. 基于同样的方式继续做“组合体检”标准工作流
2. 再做“复盘报告”标准工作流
3. 优先沉淀最能重复使用的固定路径，而不是再拆更多原子工具
4. 持续保持模板优先、原子工具兜底的接入方式

---

## 26. 本阶段新增沉淀：组合体检标准工作流

### 26.1 我们确认的原则

- 组合体检应该是盘前概览之后的第二条高价值标准路径
- 组合体检不是做更多分析，而是做更稳的健康判断与回测验证
- 体检结果要能直接服务于继续持有、收缩风险或进入复盘

### 26.2 本阶段落地内容

- 新增 `specs/SDD_PHASE20_PORTFOLIO_HEALTH_CHECK.md`
- 新增 `core/agent/workflow_templates.py` 中的 `组合体检模板`
- `core/agent/routing.py` 可识别组合体检类意图
- `core/agent/collaboration.py` 可生成固定执行顺序
- `core/agent/capability_directory.py` 增加组合体检标准路径

### 26.3 对下一阶段的建议

下一阶段最值得继续做的是：

1. 基于同样的方式继续做“复盘报告”标准工作流
2. 让盘前概览、组合体检和复盘报告成为三个固定入口
3. 继续保持模板优先、原子工具兜底的接入方式
4. 后续若要增强，再考虑统一工作台，而不是继续拆碎工具

---

## 27. 本阶段新增沉淀：复盘报告标准工作流

### 27.1 我们确认的原则

- 复盘报告应该是收口型工作流，而不是再造一个新的分析引擎
- 最有价值的做法不是继续堆算法，而是把回放、反馈、学习统计串成稳定链路
- 复盘要先给结果，再给依据，最后给下一步建议

### 27.2 本阶段落地内容

- 新增 `specs/SDD_PHASE21_REVIEW_REPORT.md`
- 新增 `core/agent/workflow_templates.py` 中的 `复盘报告模板`
- `core/agent/routing.py` 可识别复盘类意图
- `core/agent/collaboration.py` 可生成固定执行顺序
- `core/agent/capability_directory.py` 增加复盘报告标准路径

### 27.3 对下一阶段的建议

下一阶段最值得继续做的是：

1. 保持盘前概览、组合体检和复盘报告三条标准工作流稳定
2. 优先增强统一输出、审计字段和报告质量，而不是再拆新工具
3. 如果后续要继续扩展，优先考虑按场景细分报告模板
4. 继续保持“规格 - 实现 - 归档”的节奏，不回到局部无限优化

---

## 28. 本阶段新增沉淀：标准工作流总入口

### 28.1 我们确认的原则

- 既然三条标准工作流已经成型，就应该给它们一个更短的生产入口
- `answer_decision_request` 是最适合承接这个职责的统一入口
- 入口越少，智能体越容易稳定接入，用户越容易快速使用

### 28.2 本阶段落地内容

- 新增 `specs/SDD_PHASE22_STANDARD_WORKFLOW_PORTAL.md`
- `core/orchestrator.py` 支持把标准入口收束到工作流执行
- `core/agent/routing.py` 可识别标准工作流总入口语义
- `core/agent/capability_directory.py` 把标准入口前置为推荐路径
- `core/agent/serialization.py` 将 `answer_decision_request` 统一归入 workflow 分类

### 28.3 对下一阶段的建议

下一阶段最值得继续做的是：

1. 保持标准工作流总入口稳定可用
2. 继续增强统一输出里的业务摘要、风险和下一步建议
3. 如果后续继续扩展，只围绕现有标准工作流做场景细分
4. 继续保持“规格 - 实现 - 归档”的节奏，避免入口再次碎片化

---

## 29. 本阶段新增沉淀：最小反馈闭环

### 29.1 我们确认的原则

- 真实测试场景里，反馈入口应该尽可能短
- 采纳 / 不采纳 比复杂反馈表单更适合第一阶段使用
- 如果已经有 `session_id`，就应该尽量自动回推 `workflow_id`

### 29.2 本阶段落地内容

- 新增 `specs/SDD_PHASE23_MINIMAL_FEEDBACK_LOOP.md`
- `DecisionSessionStore.submit_feedback()` 支持从 `session_id` 回推 `workflow_id`
- `StockOrchestrator.submit_decision_feedback()` 补齐编排层反馈入口
- `POST /feedback` 允许只传 `session_id`
- `examples/api_demo.py` 反馈流程已放宽

### 29.3 对下一阶段的建议

下一阶段最值得继续做的是：

1. 在真实使用中多记录几次采纳/不采纳反馈
2. 观察哪些建议最容易被采纳、哪些最容易失效
3. 再基于真实反馈，逐步优化算法、工具和工作流
4. 继续保持“先可用，再改进”的节奏，不要过度工程化
