# Phase 16 最小 curl 使用示例归档

---

## 1. 归档结论

Phase 16 已完成最小 `curl` 使用示例，项目现在可以更方便地在终端直接调试 API。

---

## 2. 已完成内容

- 新增 Phase 16 规格草案
- README 已补充最小 `curl` 示例
- 覆盖健康检查、决策和反馈调用

---

## 3. 当前收口边界

本阶段只补最小终端示例，不引入额外文档体系或客户端库。

---

## 4. 轻量验证要求

建议验证：

1. 可以直接复制 `curl` 示例
2. 可以请求 `/health`
3. 可以请求 `/decision`
4. 可以请求 `/feedback`

---

## 5. 后续建议

下一阶段最值得继续推进的是：

1. 如果准备生产部署，再补最小部署模板
2. 如果要真实联东方财富，再补数据联调说明
3. 如果要更可视化，再补简单结果展示页
