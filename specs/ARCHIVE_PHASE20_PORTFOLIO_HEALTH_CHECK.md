# 归档文档：Phase 20 组合体检标准工作流

---

## 1. 归档结论

Phase 20 已完成第二个高价值标准工作流：组合体检。

该工作流已经可以稳定命中模板，并按固定顺序组织：

1. 市场概览
2. 风控约束
3. 运行健康
4. 回测验证

---

## 2. 已完成内容

- 新增 Phase 20 规格草案
- 新增 `组合体检模板`
- 扩展共享路由，识别“组合体检 / 组合检查 / 策略体检”等意图
- 扩展协作规划语义与执行顺序
- 扩展能力目录，加入组合体检标准路径
- 完成轻量验证

---

## 3. 验证结果

轻量验证已通过：

- `parse_intent("帮我做一下组合体检")` 命中 `portfolio_health_check`
- `select_workflow_template(...)` 命中 `组合体检模板`
- `build_collaboration_plan(...)` 输出模板名 `组合体检模板`
- 执行顺序为 `['market', 'risk', 'health', 'backtest']`

---

## 4. 当前收口边界

本阶段只做组合体检标准工作流，不扩展组合优化器、不新增资产配置模型、不接实盘调仓。

---

## 5. 本阶段结论

组合体检是第二条非常合适的标准工作流，因为它补上了盘前概览之后的“是否还健康”的判断链路。

---

## 6. 后续建议

1. 下一步做“复盘报告”标准工作流
2. 保持模板优先、原子工具兜底的接入方式
3. 如果后续需要，再把盘前概览和组合体检合并成一个更大的日常工作台入口
