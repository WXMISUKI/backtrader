# Phase 14 最小 API 联调用例归档

---

## 1. 归档结论

Phase 14 已完成最小联调用例，项目现在可以更容易地验证最小 HTTP API 的健康检查、统一决策和反馈闭环。

---

## 2. 已完成内容

- 新增 Phase 14 规格草案
- 新增 `examples/api_demo.py`
- 覆盖 `GET /health`
- 覆盖 `POST /decision`
- 可选覆盖 `POST /feedback`

---

## 3. 当前收口边界

本阶段只提供最小联调用例，不引入测试框架、不引入第三方 HTTP 客户端，也不扩展业务逻辑。

---

## 4. 轻量验证要求

建议验证：

1. `examples/api_demo.py` 可以正常启动
2. 可以成功访问 `/health`
3. 可以成功访问 `/decision`
4. 如启用反馈，可以成功访问 `/feedback`

---

## 5. 后续建议

下一阶段最值得继续推进的是：

1. 补一个最小部署说明
2. 将联调方式写入 README 或使用指南
3. 在真实环境里做一次端到端演练
