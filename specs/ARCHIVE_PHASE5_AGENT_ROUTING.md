# 归档文档：Phase 5 智能体意图路由

## 文档信息

| 项目 | 内容 |
|------|------|
| 文档名称 | Phase 5 智能体意图路由归档报告 |
| 版本 | v1.0 |
| 完成日期 | 2026-06-17 |
| 状态 | ✅ 已完成 |

---

## 1. 本阶段目标

把智能体工具路由从简单关键词匹配升级为结构化意图路由。

---

## 2. 完成内容

### 2.1 新增规格

- `specs/SDD_PHASE5_AGENT_ROUTING.md`

### 2.2 新增/更新实现

- `core/agent/client.py`
- `core/agent/runtime.py`

### 2.3 新增知识库沉淀

- `docs/knowledge-base/QUANT_AGENT_LEARNING_MAP.md`

---

## 3. 已实现能力

- 可将自然语言解析为结构化意图
- 可提取股票代码
- 可提取风险偏好
- 可为主要业务场景返回默认参数
- 运行时可显式查看 intent 解析结果

---

## 4. 验证结果

轻量验证已通过：

- `py_compile` 通过
- `parse_intent("帮我回测 000001")` 正常返回 `backtest`
- `parse_intent("看看市场大盘")` 正常返回 `market_overview`

---

## 5. 结论

这一步把智能体接入的“第一跳”变得更可解释了。

后续如果继续扩展多智能体协作或更复杂的对话编排，已经有了一个可复用的意图入口。

---

## 6. 下一阶段建议

1. 把 `StockOrchestrator.route()` 也逐步收敛成同一套结构化意图
2. 对复合意图做更细的优先级规则
3. 再考虑多智能体分工草案

