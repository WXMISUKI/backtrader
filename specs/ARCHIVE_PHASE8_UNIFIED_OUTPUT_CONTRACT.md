# Phase 8 统一输出字段枚举与消费契约归档

---

## 1. 归档结论

Phase 8 已完成最小闭环：项目的核心输出字段与消费契约开始收口，对外使用口径更稳定，后续扩展新工具时更不容易出现“字段各写各的”。

---

## 2. 已完成内容

- 新增 Phase 8 规格草案
- 新增统一输出契约模块 `core/agent/output_contract.py`
- 序列化层已引用统一工具输出字段契约
- 项目导出层已暴露契约常量

---

## 3. 当前收口边界

本阶段只定义和稳定核心字段，不强制改造所有历史消费者。

推荐优先消费：

- `ok`
- `tool`
- `category`
- `data_source`
- `summary`
- `data`
- `meta`
- `task_protocol`
- `governance`

---

## 4. 轻量验证要求

建议验证：

1. `build_tool_payload()` 输出结构稳定
2. `output_contract` 常量可导出
3. 编译验证通过

---

## 5. 后续建议

下一阶段最值得继续推进的是：

1. 把统一输出契约接到更多技能和报告入口
2. 让回放结果在前端或调试工具中直接复用
3. 再考虑更严格的字段校验或 schema 生成

